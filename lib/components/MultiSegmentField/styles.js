'use strict';

export default (function (theme) {
  return {
    segment: {
      fontSize: 10,
      height: 28,
      transform: 'none',
      borderRadius: 0,
      boxShadow: 'none',
      padding: '5px 0',
      minHeight: 0,
      minWidth: 0
    }
  };
});